<header>
    <!-- Navbar -->
    <nav class="navbar fixed-top navbar-expand-lg navbar-dark scrolling-navbar">
        <div class="container">
            <!-- Brand -->
            <a class="navbar-brand" href="#">
                <strong>Damien SARRAZY</strong>
            </a>

            <!-- Collapse -->
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>


            <!-- Links -->
            <div class="collapse navbar-collapse" id="navbarSupportedContent">

                <!-- Left -->
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('home_index') }}#">Home
                            <span class="sr-only">(current)</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('home_index') }}#about" >A propos de moi</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('home_index') }}#skills" >Compétences</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('home_index') }}#portfolio" >Portfolio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('home_index') }}#contact" >Contact</a>
                    </li>
                </ul>

                <!-- Right -->
                <ul class="navbar-nav ml-auto nav-flex-icons">
                    <li class="nav-item">
                        {% if app.user %}
                            <a class="nav-link" href="{{ path('project_new') }}" >Ajouter</a>
                        {% endif %}
                    </li>
                    <li class="nav-item">
                        {% if app.user %}
                            <a class="nav-link" href="{{ path('project_index') }}" >Gérer</a>
                        {% endif %}
                    </li>
                </ul>

                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        {% if app.user %}
                            {{ app.user.email }}
                            <a href="{{ path('app_logout') }}"> Se déconnecter</a>
                        {% else %}
                            <a href="{{ path('app_login') }}"> Se connecter</a>
                        {% endif %}
                    </li>
                </ul>

            </div>
        </div>
    </nav>
</header>

